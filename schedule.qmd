
```{r}
#| message: false
#| echo: false
library(tidyverse)
```


```{r}
#| echo: false
## Course specifics

slides_repo <- "https://github.com/stats6-sp26/slides/"
```


```{r}
#| echo: false
#| message: false
readr::read_csv(here::here("specific-content/schedule.csv")) |> 
  mutate(
    session = if_else(
      is.na(session), " ", session
    ),
    reading = if_else(
      is.na(reading), 
      paste0(fontawesome::fa("book", fill = "#e6ddd1")),
      paste0(
        "[", 
        fontawesome::fa("book"), 
        "]",
        "(specific-content/readings/", 
        reading, 
        ")"
        )
      ),
    slides = if_else(
      is.na(slides), 
      paste0(fontawesome::fa("laptop", fill = "#e6ddd1")),
      paste0(
        "[", 
        fontawesome::fa("laptop"), 
        "]",
        "(",
        slides_repo, 
        slides, 
        ")"
        )
      ),
     other = if_else(
      is.na(other), 
      paste0(fontawesome::fa("note-sticky", fill = "#e6ddd1")),
      paste0(
        "[", 
        fontawesome::fa("note-sticky"), 
        "]",
        "(",
        other, 
        ")"
        )
      )
  ) |> 
  knitr::kable() 
```

